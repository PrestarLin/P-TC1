# P-TC1 开发约定

## 构建方式（重要）
- **不要在本机编译固件**。使用 GitHub Actions 云端编译。
- 修改代码后直接 `git commit` + `git push origin dev`，`.github/workflows/build.yml` 会自动在云端编译并把固件（`.bin`）上传为 Dev/Release 附件。
- CI 会自动执行 `python3 test.py > web_data.c` 重新生成 web 资源，因此修改 `TC1/http_server/web/*`（如 `index.html`）后不必手动重新生成 `web_data.c`，直接提交源码即可。

## 分支
- 日常迭代推到 `dev`；`master` 分支推入会触发正式 Release。

## 涉及的自定义
- 当前分支：`dev`
- 远程：`origin`（ssh://git@ssh.github.com:443/PrestarLin/P-TC1.git）

## 编程原则（Karpathy Guidelines）
写代码前先思考，不要臆断、不要掩盖疑问，把权衡取舍讲清楚；不确定就问。

1. **先想后写**：明确陈述假设；有多种理解时全部列出而不是擅自选择；存在更简单的做法要直接说出来；不清晰就停下来提问。
2. **简单优先**：用解决问题所需的最少代码，不做没被要求的扩展、不为单一用途建抽象、不为不可能发生的场景加错误处理。
3. **外科手术式修改**：只改必须改的；不顺手"优化"无关代码/注释/格式；不重构没坏的东西；保持与现有风格一致；发现无关的死代码只提一下，不要删除。
4. **目标驱动**：把任务转成可验证的目标（如"修 bug"→"写一个能复现的测试并让它通过"）；多步骤任务先列简短计划，逐步验证。
